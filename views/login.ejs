<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            background: linear-gradient(to right, #8cff8c, #50ff50);
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            font-family: 'Arial', sans-serif;
            overflow: hidden;
        }

        .success-container {
            text-align: center;
            color: #000;
            animation: fadeIn 1.5s ease-out;
        }

        .success-message {
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 20px;
            animation: bounce 0.8s ease infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-20px);
            }
            60% {
                transform: translateY(-10px);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>
    <title>Success Page</title>
</head>
<body>
    <div class="success-container">
        <div class="success-message">تمت عملية توثيقك بنجاح✅</div>
    </div>
</body>
</html>
